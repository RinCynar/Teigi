**Teigi 项目计划执行书**  
（Flutter 跨平台媒体格式转换工具）

**版本：** 1.0  
**日期：** 2026-08-12  
**目标平台：** Windows / macOS / Linux（桌面优先，后续可扩展移动端）  
**设计风格：** Material You（动态色彩、圆角、自适应布局）  
**核心依赖：** ffmpeg（系统 PATH 或用户指定路径）

---

### 1. 项目概述

Teigi 是一款精致、高效的桌面端媒体格式转换工具。  
核心价值：

- 不局限预设格式，支持用户自定义目标格式并保存为快捷选项
- 完整队列管理 + 单文件/批量目标格式指定
- 实时进度、剩余时间估算、多线程、GPU 加速
- 根据目标格式动态展示详尽配置项
- Material You 设计 + 自适应窗口（支持从紧凑到宽屏布局）

---

### 2. 技术栈与环境

| 类别 | 技术/工具 | 说明 |
|------|-----------|------|
| 框架 | Flutter 3.22+ | 桌面优先 |
| 状态管理 | Riverpod 2.x | 推荐，或 Provider |
| 路由 | go_router | 简单清晰 |
| UI | Material 3 + 自定义主题 | 动态色、自适应布局 |
| 文件选择 | file_picker | 多文件/文件夹 |
| 路径/进程 | path_provider + process_run / dart:io Process | 调用 ffmpeg |
| 进度与时间估算 | 自定义 ProgressNotifier + 解析 ffmpeg 输出 | |
| 持久化 | shared_preferences + hive / isar | 设置、快捷格式、历史 |
| 日志 | logger | 调试与用户问题排查 |
| 打包 | msix / dmg / AppImage 或 flutter_distributor | |

**ffmpeg 策略：**
1. 启动时检测 `ffmpeg` 是否在 PATH 中可用（`which ffmpeg` / `where ffmpeg`）
2. 若未找到 → 弹窗提示安装或手动指定可执行文件路径
3. 用户可在设置中永久保存自定义 ffmpeg 路径
4. 每次转换前二次验证路径有效性

---

### 3. 核心功能模块拆解

#### 3.1 主界面布局（自适应）
- **窄窗口（< 900px）**：顶部工具栏 + 队列列表（可折叠设置面板）
- **中等窗口**：左侧队列列表 + 右侧预览/配置面板
- **宽窗口（> 1400px）**：三栏布局（队列 | 配置 | 日志/预览）
- 支持拖拽调整分割条
- Material You 动态色（跟随系统主题或自定义种子色）

#### 3.2 文件队列管理
- 支持拖拽、多选文件、文件夹导入
- 队列表格列：文件名、原格式、大小、目标格式、状态、进度、操作
- 支持一键为全部队列设置目标格式
- 支持单独修改某个文件的目标格式
- 右键菜单：移除、上移/下移、打开所在文件夹、单独开始/暂停

#### 3.3 格式与配置系统
- **内置常见格式**：视频（mp4, mkv, webm, avi, mov…）、音频（mp3, aac, flac, wav, opus…）、图片（jpg, png, webp, avif…）等
- **自定义格式**：用户手动输入扩展名（如 `hevc`、`prores`、`gif` 等），可保存为「快捷选项」
- **动态配置面板**（根据目标格式智能展示）：
  - 视频：编码器（libx264 / libx265 / h264_nvenc / hevc_nvenc / libvpx-vp9…）、CRF/质量、分辨率、帧率、像素格式、音视频流选择
  - 音频：编码器、比特率、采样率、声道、音量
  - 图片：质量、缩放、颜色空间
  - 通用：硬件加速（NVENC / QSV / AMF / VideoToolbox）、多线程数、输出目录、文件名模板、覆盖策略
- 预设模板：快速选择「高质量」「体积优先」「移动端」等，并可自定义保存

#### 3.4 转换引擎
- 基于 `Process.start` 调用 ffmpeg
- 解析 stderr 实时获取进度百分比与速度
- 剩余时间估算：基于已处理时长与平均速度（滑动窗口）
- 多线程：支持同时转换 N 个文件（可配置并发数，默认 CPU 核心数的一半）
- GPU 加速：检测并暴露可用硬件加速选项，用户可勾选
- 暂停/继续：通过发送信号或重新启动（ffmpeg 本身对暂停支持有限，建议实现为取消+从断点重试或队列优先级）
- 完成后自动打开输出目录（可选）

#### 3.5 设置项（详尽且合理）
- ffmpeg 路径管理
- 默认输出目录 / 与源文件同目录 / 自定义
- 并发数、默认硬件加速偏好
- 日志级别、自动清理临时文件
- 主题与 Material You 种子色
- 快捷格式管理（增删改排序）
- 文件名冲突策略、完成后行为
- 语言（中英优先）

---

### 4. 项目结构建议

```
teigi/
├── lib/
│   ├── main.dart
│   ├── app.dart
│   ├── core/
│   │   ├── ffmpeg/
│   │   │   ├── ffmpeg_detector.dart
│   │   │   ├── ffmpeg_runner.dart
│   │   │   ├── progress_parser.dart
│   │   │   └── hardware_accel.dart
│   │   ├── models/
│   │   │   ├── media_file.dart
│   │   │   ├── conversion_task.dart
│   │   │   ├── format_preset.dart
│   │   │   └── conversion_options.dart
│   │   └── utils/
│   ├── features/
│   │   ├── queue/
│   │   ├── settings/
│   │   ├── format_config/
│   │   └── home/
│   ├── providers/
│   ├── theme/
│   └── widgets/
├── assets/
├── test/
└── pubspec.yaml
```

---

### 5. 开发阶段与里程碑（可执行时间表）

**阶段 0：准备（1–2 天）**
- 创建 Flutter 桌面项目
- 配置 Material 3 + 自适应布局基础
- 添加依赖（riverpod、file_picker、path、logger 等）
- 实现 ffmpeg 检测与路径配置基础 UI

**阶段 1：核心骨架（3–5 天）**
- 主界面自适应布局 + 队列列表
- 多文件导入与拖拽
- 基础任务模型与状态管理
- 简单 ffmpeg 调用（固定参数测试）

**阶段 2：转换引擎（5–7 天）**
- 实时进度解析 + 进度条 + 剩余时间估算
- 多并发任务管理（限制并发数）
- 硬件加速选项检测与传递
- 错误处理与日志

**阶段 3：格式与配置系统（5–7 天）**
- 内置格式列表
- 自定义格式输入 + 快捷选项持久化
- 根据目标格式动态生成配置 UI
- 预设模板系统
- 一键批量设置目标格式 + 单文件覆盖

**阶段 4：完善与打磨（4–6 天）**
- 完整设置页面
- 主题与 Material You 动态色
- 窗口自适应细节优化
- 异常边界处理、用户提示优化
- 基础单元测试 + 手动测试清单

**阶段 5：打包与发布准备（2–3 天）**
- Windows / macOS / Linux 打包
- 用户文档与常见问题
- 首次启动引导（ffmpeg 检测）

**总预估：** 20–30 个工作日（单人全职）

---

### 6. 关键实现要点（避免踩坑）

1. **进度解析**：ffmpeg 进度在 stderr，使用正则匹配 `time=` 和 `speed=`。
2. **剩余时间**：用滑动窗口平均速度计算，避免单次波动过大。
3. **并发控制**：用 `Semaphore` 或队列 + 信号量限制同时运行的 Process。
4. **路径安全**：所有用户输入的路径做规范化与存在性检查。
5. **自定义格式**：仅保存扩展名，实际编码参数由用户在配置面板填写（或提供智能默认）。
6. **Material You**：使用 `ColorScheme.fromSeed` + 动态色支持（Windows 11 / macOS 需额外处理）。
7. **自适应**：大量使用 `LayoutBuilder` + `Breakpoint` 判断，配合 `NavigationRail` / `Drawer` 切换。

---

### 7. 风险与应对

| 风险 | 应对 |
|------|------|
| ffmpeg 输出格式变化 | 做版本检测 + 多套解析正则 |
| 大文件内存占用 | 流式处理，不一次性读全部输出 |
| 硬件加速兼容性 | 提供「自动检测」+ 手动覆盖，失败时回退 CPU |
| 桌面权限 | 明确提示用户授权文件访问 |
| 跨平台路径差异 | 统一使用 `path` 包处理 |

---

### 8. 下一步立即行动清单

1. 创建项目：`flutter create --platforms=windows,macos,linux teigi`
2. 添加核心依赖并配置 Material 3 主题
3. 实现 `FFmpegDetector` 类（PATH 检测 + 自定义路径）
4. 搭建主界面骨架 + 队列列表 UI
5. 完成第一个可运行的「单文件简单转换」闭环

---